export type PageProps = Partial<{
  display: string;
  $justifyContent: string;
  $alignItems: string;
}>;

export type MovieType = {
  id: number;
  title: string;
  director: string;
  rating: string;
};
